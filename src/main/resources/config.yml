# Minimum swept collision distance. Latency adds the player's recent horizontal
# distance per client tick to this value.
prediction-distance: 0.1

# Maximum distance used by latency-adjusted collision lookahead.
time-lookahead-max-distance: 0.6
