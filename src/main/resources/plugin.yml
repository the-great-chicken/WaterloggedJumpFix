name: WaterloggedJumpFix
version: '${version}'
main: dev.waterloggedjumpfix.WaterloggedJumpFixPlugin
description: Server-side workaround for automatic wall boosts in shallow water
api-version: '26.1.2'
load: POSTWORLD
